import makeStyles from '@material-ui/styles/makeStyles';

export const useSnippetStyles = makeStyles({
	footer: {
		textAlign: 'end',
		padding: '0 20px',
		position: 'sticky',
		bottom: 0,
		background: 'white',
	},
});
